plugins {
    id 'java'
}

// 生成jar包的时候，没有生成，且输出Task:jar skipped!
jar.enabled = true


dependencies {
    compile project(":ibook-service")
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
